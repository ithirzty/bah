#include "iostream.bah"

testIostream() bool {
    const ISTRM_SELF_PATH = "./libs/iostream.bah"


    fs = fileStream{}
    fs.open(ISTRM_SELF_PATH, "r")
    f = fs.readContent()
    fs.close()

    if f == "" {
        return false
    }

    return true
}