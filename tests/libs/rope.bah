#include "iostream.bah"
#include "rope.bah"

testRope() bool {
    as = "this is a "
    bs = "test"

    a = rope(as)
    b = rope(bs)

    c = a + b

    if c.toStr() != as+bs {
        return false
    }

    return true
}