#include "iostream.bah"
#include "string.bah"
#include "http.bah"

testHttp() bool {
    req = http_request{
        timeout: 2
    }

    req.url = parseUrl("http://example.com")
    resp = req.send()

    return resp.body.length > 0
}